# weav-ai-providers

Provider adapter package for WeavInt AI.

This package centralizes provider construction and model listing. It currently delegates to `weav_provider_router` when that package is installed.

